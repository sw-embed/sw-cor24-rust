all examples 1. no halt, jmp to self label


r0, r1, r2, sp, fp,
r3=fp r4=sp r5=z r6 iv, r7 ir  -- interrupt vector, interrupt return
never use r3, r4, r5, -

----
000000 - < code loaded here
<010000 
-
<FE0000  16M - 128K

===

     FE0000 start EBR
sp = FEEC00 - end of ebedded block RAM (bottom) 2K stack at top.


FF0000 - top 64k I/O byte access  RP=SW RW=LED (low bit)

FF0010 internet enabled - low bit

FF0100 101 (102 ignored) UART - data status - claude/verilog correct -- status s/b not-busy, CTS active, RTS
----

example 4.
could use post-increment compare with zero -- separate compare and post increment branch
--
example 5.
show more memory 
---
example 6.
condition register one bit register
---
Registers r0, r1, and r2 (fp sp, z only in compare inst x = z, z < uns ,   (< = >) all.txt

RUST 24 bit sll,sra to sign-extend
silent overflow
detected in code, wraps at 24, so a+b=c if c<a and c<b wrap occurred
---
D1 power LED
D2 user LED - default high
S1 reset sw
S2 momentary PB normal High, depressed Low momentariy
---Example 9
simplify xor to toggle
---
web ui ISA ref
jmp to 24-bit address
--
jmp dd -- absolute (vs bra dd relative, signed, pc is +4 from the inst base). "halt" could be jmp -4

--- NOP = jmp -2



