online, assem, fib:

  (new fib)
  lc r0,1
  aub r2,r0

s/b

   add r2,-1

and change counter-- to --counter

----
counter-- easier compare
   look at again.

ceq, r2,z

clu r2,z
comp -- look at email  branch true on non-zero
bra loop

---
look at online, C, fib: line 11

rate limiter for emulator

uart_putc
   check for ready

---
button pulled high
LED pulled high

Blink example invert unnecearray, emulator sw/led one is wrong
---

ssembler example Echo also needs to check for TX busy

ff0101 status reg bit 7 busy -- see email,  load byte compare 0, bra lt zero cls bra if true
---
buttons on left are not wide enough

